import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Introduction Call — Americurial Federal Capture Consulting",
  description:
    "20-minute introduction call guide for potential consulting clients. Qualification conversation, business assessment, and next steps.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
